# Write a for loop that iterates over a list of strings lst and prints the first three characters of every word. If lst is the list ['January', 'February', 'March'] then the
# following should be printed:
# Jan
# Feb
# Mar

lst = ['August', 'September', 'October']
for word in lst:
    print(word[:3])
